import type {
  PatientDataModel,
  CasesModel,
  SeriesModel,
  SopModel,
} from "./patientDataModal";

// store

// import type RootState from "../store";

export type { PatientDataModel, CasesModel, SeriesModel, SopModel };
